<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4"
      main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode name="NavigateRecovery"
                  number_of_retries="6">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode name="ComputePathToPose"
                        number_of_retries="1">
            <Sequence>
              <!-- planner_id: AStar | RRTStar | HybridAStar -->
              <ComputePathToPose goal="{goal}"
                                 path="{raw_path}"
                                 planner_id="AStar"/>
              <!-- smoother_id: simple_smoother | esdf_g2o_smoother -->
              <SmoothPath smoother_id="simple_smoother"
                          unsmoothed_path="{raw_path}"
                          smoothed_path="{candidate_path}"/>
            </Sequence>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context"
                                service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <ManageTrajectory input_path="{candidate_path}"
                          output_path="{path}"
                          goal="{goal}"
                          replan_event_topic="trajectory_generation/replan_event"/>
        <!-- controller_id: FollowPath (mecanum TEB) | MPC | MPPI | DWA -->
        <FollowPath path="{path}"
                    controller_id="MPPI"/>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <Sequence name="StuckRecovery">
          <BackUp backup_dist="0.2"
                  backup_speed="0.05"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Subtree"
                              service_name="local_costmap/clear_entirely_local_costmap"/>
          <Wait wait_duration="2.0"/>
        </Sequence>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Action ID="BackUp"
            editable="true">
      <input_port name="backup_dist"/>
      <input_port name="backup_speed"/>
    </Action>
    <Action ID="ClearEntireCostmap"
            editable="true">
      <input_port name="service_name"/>
    </Action>
    <Action ID="ComputePathToPose"
            editable="true">
      <input_port name="goal"/>
      <input_port name="path"/>
      <input_port name="planner_id"/>
    </Action>
    <Action ID="FollowPath"
            editable="true">
      <input_port name="path"/>
      <input_port name="controller_id"/>
    </Action>
    <Action ID="GoalUpdated"
            editable="true"/>
    <Action ID="ManageTrajectory"
            editable="true">
      <input_port name="input_path"/>
      <output_port name="output_path"/>
      <input_port name="goal"/>
      <input_port name="replan_event_topic"/>
    </Action>
    <Control ID="PipelineSequence"
             editable="true"/>
    <Decorator ID="RateController"
               editable="true">
      <input_port name="hz"/>
    </Decorator>
    <Control ID="RecoveryNode"
             editable="true">
      <input_port name="number_of_retries"/>
    </Control>
    <Action ID="SmoothPath"
            editable="true">
      <input_port name="smoother_id"/>
      <input_port name="unsmoothed_path"/>
      <output_port name="smoothed_path"/>
    </Action>
    <Action ID="Wait"
            editable="true">
      <input_port name="wait_duration"/>
    </Action>
  </TreeNodesModel>

</root>
